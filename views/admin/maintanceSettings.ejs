<%- include("../partials/header") %>
<div class="content">
    <div class="container-fluid">
      <div class="row">
        <div class="col-md-5">
          <div class="card">
            <div class="card-header card-header-primary">
              <h4 class="card-title">Bakım Modu</h4>
            </div>
            <div class="card-body">
              <form method="POST">
                <div class="row">
                  <div class="col-md-12">
                    <div class="form-group">
                      <label>Mesaj</label>
                      <div class="form-group">
                        <textarea class="form-control" name = "mesaj" rows="5"><%= oyun_setting.Bakim_Mesaj %></textarea>
                      </div>
                    </div>
                  </div>
                  <div class="col-md-2"><br>
                    <div class="form-group">
                      <label for="exampleFormControlSelect1">Durum</label>
                      <select class="form-control" name="status" id="exampleFormControlSelect1">
                         <option class = "text-dark"><%= oyun_setting.Bakim_Status %></option>
                        <% if(oyun_setting.Bakim_Status === "Aktif"){ %>
                          <option class="text-dark">Pasif</option>
                        <% } else { %>
                          <option class="text-dark">Aktif</option>
                        <% } %>
                      </select>
                    </div>
                  </div>
                </div>
                <button type="submit" class="btn btn-primary pull-right">Güncelle</button>
                <div class="clearfix"></div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
      <%- include("../partials/footer") %>